apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: airflow-api-server
  namespace: airflow
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: airflow.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: airflow-api-server
            port:
              number: 8080